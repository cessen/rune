#const a: i32 = 42
#var c: i32 #= 31

const foo = fn [x: i32, y: i32] -> i32 (
    return x + y
)

fn main[] -> i32 (
    #print["%d, %d, %d\n", a+b, b+c, foo[a, b]]
	val a: i32 = 42
	val b: i32 = 5
	b = 0
	return foo[a, b]
)
